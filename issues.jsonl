{"id": 1, "summary": "Dialogue phrase counter cuts scenes short", "description": "Author's own note at OPIL-source.bas:873 - 'DEFEITO NO CONTADOR DE FRASE, CORTA RAPIDO E NAO TERMINA'. FIXED - user confirmed the rebuilt demo plays correctly on 2026-08-05. The earlier hypothesis (DEC z at the loop top vs EXIT IF z=0 at the bottom) turned out to be wrong: the arithmetic is sound and z initial IS the sentence count. Two real defects instead. (1) CORTA RAPIDO: z=RND(8)+1 with IF z>7 THEN z=7 folded 8 onto the one-line solo sentinel 7, giving it 2/8 of the probability mass; 3 in 8 scenes were a single sentence. Now z=RND(7)+1 with no clamp, uniform 1-7. (2) NAO TERMINA: the dialogue area was cleared before the closing WAIT #1500, erasing the last sentence instantly and leaving the scene blank; the clear was also redundant since CLS follows. Removed. Rebuilt and verified under XRoar.", "type": "bug", "status": "done", "priority": "high", "file": "OPIL-source.bas", "created": "2026-08-05", "updated": "2026-08-05", "tags": ["dialog", "logic"]}
{"id": 2, "summary": "Three of four cutscene categories are dead code", "description": "OPIL-source.bas:587 reads 'z=0 :\\'z=RND(3)' - the category randomizer is commented out, so only the z=0 skyline branch ever executes. The four backgrounds (midl/dock/citi/spac) are all still reachable via the sc chooser inside that branch, so this is about the three unwritten scene categories, not unreachable art. Decide whether to author them or remove the scaffolding. Blocked on issue 6.", "type": "enhancement", "status": "open", "priority": "medium", "file": "OPIL-source.bas", "created": "2026-08-05", "updated": "2026-08-05", "tags": ["cutscene", "dead-code"]}
{"id": 3, "summary": "Document a reliable XRoar invocation", "description": "Verified working: xroar -machine coco2bus -cart rsdos -load-fd0 OPIL-EN.dsk -ao null -timeout 120 -type 'RUN\"LOADER\"\\r'. Key findings: -cart rsdos is mandatory, coco2bus (NTSC) not coco2b (PAL) because WAIT pacing assumes 60Hz, and -type needs \\r not \\n. Written up in the README 'Running it' section. Confirmed by the user on 2026-08-05 via 'make run', which uses this exact invocation, playing the full demo end to end. Screenshot work split out to issue 7.", "type": "task", "status": "done", "priority": "medium", "file": "README.md", "created": "2026-08-05", "updated": "2026-08-05", "tags": ["xroar", "docs"]}
{"id": 4, "summary": "object: routine has only two props", "description": "Only the briefcase (z=0) and safe (z=1) are implemented, selected by z=RND(2). A block of blank lines before the RETURN at OPIL-source.bas:583 marks where more props were planned. The safe's lights-down/lights-up index trick is a good template for adding more. Blocked on issue 6.", "type": "enhancement", "status": "open", "priority": "low", "file": "OPIL-source.bas", "created": "2026-08-05", "updated": "2026-08-05", "tags": ["object", "content"]}
{"id": 5, "summary": "Portrait arrays over-dimensioned by 18 bytes each", "description": "All ten portraits are DIM'd at 176 bytes but the draw loops in drawchrl/drawchrr only reach index 14+9*16=158. Roughly 170 bytes wasted across ten characters. Harmless today; worth reclaiming only if the program approaches a memory ceiling.", "type": "cleanup", "status": "open", "priority": "low", "file": "OPIL-source.bas", "created": "2026-08-05", "updated": "2026-08-05", "tags": ["memory"]}
{"id": 6, "summary": "No build pipeline - .dsk images cannot be rebuilt from source", "description": "RESOLVED - user confirmed 'make run' builds and plays the full demo end to end on 2026-08-05. Added a Makefile that builds the ugBASIC toolchain (ugbc.coco, asm6809, decb) and compiles with: ugbc.coco -C <asm6809> -b <decb> -o build/OPIL.dsk -O dsk OPIL-source.bas. -O dsk reproduces the LOADER.BAS + P/P.00/P.01 layout of the shipped images, confirming how they were built. Four Darwin-specific fixes were needed: Homebrew bison 3 ahead of the system's 2.3, gsed for GNU sed -i, renaming ugbc's encrypt() to avoid the unistd.h collision, and purging the prebuilt Linux x86-64 objects in the ToolShed module. Rebuilt image boots and runs correctly under XRoar; it is not byte-identical to the shipped one (same size, ~20700 bytes differ) because the originals used an older ugbc.", "type": "task", "status": "done", "priority": "high", "file": "Makefile", "created": "2026-08-05", "updated": "2026-08-05", "tags": ["build", "ugbasic", "blocker"]}
{"id": 7, "summary": "Capture a screenshot of a dialogue box and of a scrolling cutscene", "description": "docs/screens/ has the ugBASIC splash, title card, a two-character dialogue and a mid-draw solo scene, but not a speech-bubble frame or a HSCROLL cutscene - the two most illustrative shots for the README. Bubbles appear only briefly and the capture kept racing them. Consider driving XRoar with a longer -timeout and sampling on a tighter interval.", "type": "task", "status": "open", "priority": "low", "file": "docs/screens", "created": "2026-08-05", "updated": "2026-08-05", "tags": ["xroar", "docs"]}
{"id": 8, "summary": "Scene director IFs fall through, so c counts iterations not scenes", "description": "In the main loop the three scene tests are sequential IFs, not an ELSE chain. When the cutscene block sets sc=1, the following IF sc=1 fires in the same iteration, so one pass can play cutscene -> dialogue -> object. This looks intentional (it produces multi-beat sequences) but it means INC c counts loop iterations rather than scenes, so 'IF c=10 THEN GOTO intro' restarts the intro much sooner than ten scenes. Confirm against the author's intent before changing anything.", "type": "question", "status": "open", "priority": "low", "file": "OPIL-source.bas", "created": "2026-08-05", "updated": "2026-08-05", "tags": ["dialog", "logic"]}
{"id": 9, "summary": "Makefile 'toolchain' target has not been tested from a clean clone", "description": "Each toolchain build step was run and verified by hand, and the resulting compiler produces a working .dsk, but the Makefile's own toolchain target was never exercised end-to-end from an empty .toolchain/ directory - the working tree was populated by the manual runs and then moved into place. The clone, submodule init, encrypt patch and three sub-builds should be verified in one go on a clean machine. Roughly a 30 minute build.", "type": "task", "status": "open", "priority": "medium", "file": "Makefile", "created": "2026-08-05", "updated": "2026-08-05", "tags": ["build", "verification"]}
{"id": 10, "summary": "Speech/thought bubble tail comments look inverted", "description": "RESOLVED by evidence. The manual's own screenshots settle it: the solo scene (one character, in-loop z=6) renders separate dots trailing up - a THOUGHT balloon - while the two-character scene renders a solid stepped tail - a SPEECH balloon. So the branch commented ':REM DIALOG' is in fact the thought balloon and vice versa. The comments are swapped; the POKEs are correct. Documented in docs/MAKE-YOUR-OWN-STORY.md with a warning not to 'fix' the pokes to match the comments. Left the source comments as-is to keep the diff honest to the author's file.", "type": "question", "status": "done", "priority": "low", "file": "OPIL-source.bas", "created": "2026-08-05", "updated": "2026-08-05", "tags": ["dialog", "comments"]}
{"id": 11, "summary": "Adopt the baseline contribution policy", "description": "Stamped the canonical baseline CONTRIBUTING.md into the repo at the user's request, filling <PROJECT> as OPEN PILANTRA and <TRACKER> as issues.jsonl with a note on its JSONL schema. Bootstrap exception recorded plainly: this issue was filed alongside the change rather than before it, since the user asked for the stamp directly. The tracker itself already existed, so this was not a genesis moment for it. The stamped copy is the project's to edit and overrides the baseline; project-specific rules belong here, never back in the template. Left open pending maintainer rubber-stamp.", "type": "task", "status": "open", "priority": "medium", "file": "CONTRIBUTING.md", "created": "2026-08-05", "updated": "2026-08-05", "tags": ["process", "docs"]}
{"id": 12, "summary": "Extract dialogue from OPIL-source.bas to JSON and back", "description": "DONE. story/dialogue.json is the build's source of truth for text. tools/dialogue.py emit renders the ten label+DATA blocks from it; strip produces src/opil.bas, the source minus its DATA blocks plus one INCLUDE. Both outputs are gitignored and the build only ever reads OPIL-source.bas, never writes it. INCLUDE was proved safe before being relied on, since RESTORE reads across it: the same program compiled with DATA inline and with it included emits byte-identical code (2056 instructions on a reduced case, differing only in '; L:n' comments; 6720 on the full demo with one benign peephole difference). lint enforces the authoring constraints and emit refuses to run when it fails. An earlier version also kept the .bas in sync and checked for divergence - removed as unwanted scope.", "type": "task", "status": "done", "priority": "medium", "file": "tools/", "created": "2026-08-06", "updated": "2026-08-06", "tags": ["toolchain", "dialogue", "build"]}
{"id": 13, "summary": "Extract SG4 image arrays to PNG and back", "description": "Second step of the host-side toolchain, following the dialogue extractor. Convert the DIM byte arrays in OPIL-source.bas to PNGs and back, so art can be previewed and diffed. Same correctness bar: byte-exact round trip. Known complication - the arrays do not carry their own shape. Stride lives in the draw loop, not the DIM, so the converter needs a name->width table derived from the source: portraits 176 bytes stride 16 (15 columns drawn), backgrounds 896 stride 64, suitA 288 stride 32, suitB 168 stride 14, safe 285 stride 19, ugb 256 stride 32, til 98 stride 14. Each SG4 tile is 2x2 pixels so a portrait is only 32x22 pixels; keep PNGs 1:1 for clean round-tripping and leave scaling to the viewer. Palette RGB should be sampled from the captures in docs/screens/ rather than guessed.", "type": "task", "status": "open", "priority": "medium", "file": "tools/", "created": "2026-08-06", "updated": "2026-08-06", "tags": ["toolchain", "graphics", "build"]}
{"id": 14, "summary": "No working way to observe the emulated screen from a script", "description": "Screen capture via MCP started failing mid-session ('Timeout waiting for capture frame') for both window and full-display capture, which is how on-screen changes have been verified until now. Likely macOS Screen Recording permission needing re-grant. Wrote tools/screenpeek.py to read video RAM at $0400 through XRoar's GDB stub instead - it decodes the 32x16 text screen and can poll for a string. It works for a single one-shot read, but attaching to the stub HALTS the CPU, and the continue/interrupt sequence needed to sample a running program is not yet right: a plain 'c' then 0x03 breaks the connection. Either fix the GDB handshake or restore screen capture. Worth finishing - reading video RAM is a better instrument than screenshots: deterministic, greppable, and scriptable for regression checks.", "type": "task", "status": "open", "priority": "medium", "file": "tools/screenpeek.py", "created": "2026-08-06", "updated": "2026-08-06", "tags": ["tooling", "xroar", "verification"]}
